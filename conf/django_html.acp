[default]
projectname = django

[autostring]
{% block<space> = "{% block !^ %}{% endblock %}"
{%<space> = block,comment,cycle,debug,extends,filter,firstof,for,if,ifchanged,ifequal,ifnotequal,load,now,regroup,ssi,templatetag,widthratio,
